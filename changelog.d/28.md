section: Bug fixes
- [user] `pike add`/`--sync` appending to a notes file whose final line ends in two or more carriage returns no longer silently drops a `\r` from that last line: the appended line is separated with a `\r\n` so the preceding line's scanner-visible content is left unchanged (#28).
