section: Bug fixes
- [user] A HEY sync failure whose stderr carries a JSON success line (`ok:true`) before the real diagnostic now reports that diagnostic instead of an empty `hey:` message (#26).
- [user] HEY sync errors built from a raw stderr diagnostic (e.g. a backtrace on an unauthenticated exit) are now collapsed to a single, length-bounded line so they fit the status bar (#26).
