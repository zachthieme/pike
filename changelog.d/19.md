section: Bug fixes
- [user] HEY sync now rolls back the new Todo when a Re-create's @hey rewrite is refused (a line changed since the scan), so a stale edit no longer leaks a duplicate Todo that the next Sync imports as a second Task (#19).
- [user] A task line carrying two @hey tags is no longer falsely reported as an orphaned link when one of its ids already has a sync state entry; the ambiguous line produces only its single warning and its snapshot is preserved (#19).
