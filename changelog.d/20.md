section: Bug fixes
- [user] HEY Sync: a Todo renamed in HEY to a title containing "@" text — an email address like `bob@example.com`, a `@rent`, or an `@due(2026-01-01)` — is now written into the note line with the same encoding imports use, so the retitled line parses with only its original tags (and keeps exactly one `@due`) instead of planting a stray tag that the next Sync misread as a change and re-created with a mangled title. State records HEY's title as HEY holds it, and a combined rename-plus-reschedule re-creates the Todo with HEY's decoded title (#20).
