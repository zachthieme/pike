section: Bug fixes
- [user] A non-mapping `hey:` config value (`hey: false`, a string, a number, or a sequence) is now a config error naming `hey:` instead of silently enabling sync with defaults — remove the `hey:` block to disable sync (#21).
- [user] HEY sync errors now surface HEY's own message and hint even when its error envelope is pretty-printed across several lines, and a non-zero `hey` exit with no envelope now includes stderr's text alongside the exit status (#21).
