section: Documentation
- [user] Documented that a HEY title containing a newline, carriage return, or tab is folded to a single line before it is written into the notes, beside the README's `@`-encoding note (#33).
- [user] The README's state-reset remedy now tells you to list outstanding orphans and pending deletes with `pike --sync --dry-run --json`, clear them in HEY, and confirm the sync reported no failures before deleting the state file (#33).
- [user] Added the TUI new-task key (`i`) to the README's Actions table, and listed `create_task` and `toggle_collapse` in the remappable-actions list and in the action-name comment of the config pike writes on first run (#33).
- [user] Corrected the #28 changelog entries, which cited a non-existent `pike add` command, to name the actual append paths (`--sync` import and the TUI new-task action) (#33).
